[package]
name = "polynomial"
version = "0.1.0"
edition = "2021"

[dependencies]
num-traits = "0.2"
algebraic = { path = "../algebraic" }
serde = { version = "1", features = ["derive"], optional = true }
num-complex = "0.4.6"
common = { path = "../common" }

[features]
serde = ["dep:serde"]
