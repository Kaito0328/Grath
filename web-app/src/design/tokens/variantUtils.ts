// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function getTextVariantClasses(_baseColor: string, _variants: unknown[]) {
    return "";
}

export enum ColorVariantKey {
    Hover = "Hover",
    Focus = "Focus",
    Active = "Active",
}
